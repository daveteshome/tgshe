export const toCents = (s: string) => {
  const n = Number(String(s).trim().replace(/[^\d.]/g, ''));
  if (!isFinite(n)) throw new Error('Invalid price');
  return Math.round(n * 100);
};









































































































































